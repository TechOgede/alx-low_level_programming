/**
 * main - Main vehicle for programs in C
 *
 * Description: C requires all programs be run in function
 * Return: 0 if successful, error if not.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
