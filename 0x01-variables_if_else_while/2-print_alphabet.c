#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: (0) if successful
 */

int main(void)
{
	char ch;
	char *str;

	str = "abcdefghijklmnopqrstuvwxyz"
	for (i = 0; i < 26; i++)
	putchar(str[i]);
	return (0);
}
